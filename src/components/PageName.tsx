export const PageName = ({ pageTitle }: { pageTitle: string }) => (
  <h1 className="text-center text-primary text-2xl underline decoration-secondary mt-5 mb-5 object-cover">
    {pageTitle}
  </h1>
)
